STEPS TO FOLLOW:

1. HOST THE Test.js file for the backend server.
2. Run the index.html file over an http-server not just locally starting by opening the html file (it need to be started by an http-service)
    as CORs (cross origin requests) won't be allowed.
3. start the mongo server over the port its default port no: 27017
